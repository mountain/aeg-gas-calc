[project]
name = "gas"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "matplotlib",
    "scipy",
    "gassim",
    "pandas>=2.3.2",
]
